(()=>{const MARK_CLASS="__jev_highlight__";function visibleText(){const walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT,{acceptNode(node){const p=node.parentElement;if(!p||["SCRIPT","STYLE","NOSCRIPT","SVG"].includes(p.tagName)||!node.nodeValue.trim())return NodeFilter.FILTER_REJECT;return NodeFilter.FILTER_ACCEPT}});let text="",node;while((node=walker.nextNode()))text+=node.nodeValue.replace(/\s+/g," ")+"\n";return text}function highlightText(target){document.querySelectorAll("."+MARK_CLASS).forEach(el=>{const parent=el.parentNode;while(el.firstChild)parent.insertBefore(el.firstChild,el);parent.removeChild(el)});const wanted=String(target.text||"").trim();if(!wanted)return;const walker=document.createTreeWalker(document.body,NodeFilter.SHOW_TEXT);let node;while((node=walker.nextNode())){const parent=node.parentElement;if(!parent||["SCRIPT","STYLE","NOSCRIPT","SVG"].includes(parent.tagName))continue;const original=node.nodeValue,index=original.toLowerCase().indexOf(wanted.toLowerCase());if(index===-1)continue;const mark=document.createElement("mark");mark.className=MARK_CLASS;mark.textContent=original;mark.style.background="#fff1a8";mark.style.borderRadius="4px";node.parentNode.replaceChild(mark,node);mark.scrollIntoView({behavior:"smooth",block:"center"});return}const wantedNorm=wanted.replace(/\s+/g," ").toLowerCase();const elements=[...document.body.querySelectorAll("p,li,td,th,article,section,div")];const el=elements.find(e=>(e.innerText||"").replace(/\s+/g," ").trim().toLowerCase().includes(wantedNorm));if(el){el.style.outline="3px solid #f2c94c";el.style.backgroundColor="#fff9d8";el.scrollIntoView({behavior:"smooth",block:"center"});setTimeout(()=>{el.style.outline="";el.style.backgroundColor=""},5000)}}chrome.runtime.onMessage.addListener((msg,_sender,sendResponse)=>{if(msg?.type==="GET_PAGE_CONTENT"){sendResponse({content:visibleText()});return true}if(msg?.type==="HIGHLIGHT_MATCH"&&msg.match){highlightText(msg.match);sendResponse({ok:true});return true}})})();